# Rules for DICHVIDEO Project

## Automatic Git Push
Whenever you successfully complete code modifications, implement new features, or fix bugs, you MUST automatically commit those changes and push them to the GitHub repository (`origin main`) without waiting for the user to explicitly ask you to do so. 
- Example workflow: `git add .`, `git commit -m "Auto-commit: [Summary of changes]"`, `git push`.
